/** Barrel for the search feature — autocomplete hook + dropdown UI. */
export { SearchDropdown, type SearchDropdownProps } from './SearchDropdown';
export { useSearch, searchKeys, type UseSearchResult } from './useSearch';
